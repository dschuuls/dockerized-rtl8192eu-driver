version: '2.1'

services:
  rtl8192eu-driver:
    build: .
    restart: always
    network_mode: host
    privileged: true
    labels:
      io.balena.features.firmware: '1'
      io.balena.features.kernel-modules: '1'
